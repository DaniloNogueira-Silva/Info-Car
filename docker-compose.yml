services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: info-car-api
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      sqlserver:
        condition: service_healthy
      sqlserver-init:
        condition: service_completed_successfully
      redis:
        condition: service_started
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - info-car-network

  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    container_name: info-car-worker
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      rabbitmq:
        condition: service_healthy
      mongodb:
        condition: service_started
      redis:
        condition: service_started
    restart: unless-stopped
    networks:
      - info-car-network

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: info-car-sqlserver
    ports:
      - '1433:1433'
    environment:
      ACCEPT_EULA: 'Y'
      MSSQL_SA_PASSWORD: 'YourStrong!Passw0rd'
    volumes:
      - sqlserver_data:/var/opt/mssql
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "YourStrong!Passw0rd" -C -Q "SELECT 1" || exit 1
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - info-car-network

  redis:
    image: redis:7-alpine
    container_name: info-car-redis
    ports:
      - '6380:6379'
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - info-car-network

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: info-car-rabbitmq
    ports:
      - '5672:5672'
      - '15672:15672'
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - info-car-network

  mongodb:
    image: mongo:7
    container_name: info-car-mongodb
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db
    networks:
      - info-car-network
  
  sqlserver-init:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: info-car-sqlserver-init
    depends_on:
      sqlserver:
        condition: service_healthy
    command: >
      /opt/mssql-tools18/bin/sqlcmd -S sqlserver -U sa -P "YourStrong!Passw0rd" -C -Q "IF DB_ID('info-car') IS NULL CREATE DATABASE [info-car];"
    networks:
      - info-car-network

volumes:
  sqlserver_data:
  redis_data:
  rabbitmq_data:
  mongodb_data:

networks:
  info-car-network:
    driver: bridge